{
  "name": "er-swap",
  "version": "0.1.0",
  "private": true,
  "description": "ER attending shift-swap workflow (Google Apps Script bound to the schedule Sheet)",
  "scripts": {
    "test": "node --test test/*.test.js",
    "push": "clasp push",
    "open": "clasp open",
    "preview": "node preview/server.js",
    "logs": "clasp logs"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
